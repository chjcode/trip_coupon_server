<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.trip_coupon.domain.board.entity.BoardDAO">

    <resultMap id="BoardResultMap" type="Board">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>

        <association property="user" javaType="User">
            <id property="id" column="user_id"/>
            <result property="nickname" column="user_nickname"/>
            <result property="email" column="user_email"/>
            <result property="password" column="user_password"/>
            <result property="role" column="user_role"/>
            <result property="registeredAt" column="user_registeredAt"/>
        </association>
    </resultMap>

    <insert id="saveBoard" parameterType="Board">
        INSERT INTO board (user_id, title, content)
        VALUES (#{user.id}, #{title}, #{content})
    </insert>
    
    <update id="updateBoard" parameterType="Board">
        UPDATE board
        SET title = #{title},
            content = #{content},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteBoard" parameterType="Long">
        DELETE FROM board WHERE id = #{id}
    </delete>

    <select id="selectBoard" resultType="Board" parameterType="Long">
        SELECT
            b.id, b.title, b.content, b.created_at, b.updated_at,
            u.id AS user_id, u.nickname AS user_nickname, u.email AS user_email,
            u.password AS user_password, u.role AS user_role, u.registered_at AS user_registeredAt
        FROM board b
        JOIN user u ON b.user_id = u.id
        WHERE b.id = #{id}
    </select>

</mapper>